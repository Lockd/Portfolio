import React from "react";
import { ABOUT_TEXT } from "@/utils/strings";

const MainContent = () => {
  return (
    <main>
      <div className="about">{ABOUT_TEXT}</div>
    </main>
  );
};

export default MainContent;
